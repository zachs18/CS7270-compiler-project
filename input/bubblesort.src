extern fn bubble_sort(arr: *mut u64, len: u64) {
    while len > 0 {
        for i in 1..len {
            if arr[i-1] > arr[i] {
                let tmp = arr[i];
                arr[i] = arr[i-1];
                arr[i-1] = tmp;
            }
        }
    }
}
