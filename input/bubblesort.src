extern fn bubble_sort(arr: *mut u32, len: usize) {
    if len > 0 {
        for i in 1..len {
            if arr[i-1] > arr[i] {
                let tmp = arr[i];
                arr[i] = arr[i-1];
                arr[i-1] = tmp;
            }
        }
    }
}
